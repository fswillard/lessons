# Lessons

To run the local server:

```bash
docsify serve docs
```

To run the samples projects:

```bash
python3 samples/bookstore/bookstore.py

# or

python3 samples/bookstore_oop/bookstore.py
```
